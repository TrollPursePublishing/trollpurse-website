version: 0.1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    files:
      - ./**/*
    discard-paths: yes
    baseDirectory: built
  cache:
    paths:
      - ./node_modules
        
